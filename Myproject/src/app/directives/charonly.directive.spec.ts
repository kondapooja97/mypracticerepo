import { CharonlyDirective } from './charonly.directive';

describe('CharonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CharonlyDirective();
    expect(directive).toBeTruthy();
  });
});
